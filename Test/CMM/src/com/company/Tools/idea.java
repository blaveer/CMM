package com.company.Tools;

public class idea {
    /**

    负数必须有小括号括着，不然认为是错误的或者是正数之间的减法

     */
    /**
     * 关于如何让实现数组的类似于int a[]；这样的声明的问题，可以为标识符a添加一个其他类型的子节点
     * */
    /**数组只有对其整体复制后才算初始化*/
    /***关于write和read函数的问题，
     * read函数应当只有一个参数，那就是一个标识符，然后将传进来的数据赋给这个标识符
     * write应当只能是一个标识符，标识符中存有要输出的值
     * 不为别的，只为简单起见*/
}
